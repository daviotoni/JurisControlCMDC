{
  "name": "juriscontrol",
  "version": "1.0.0",
  "private": true,
  "description": "Ferramentas de desenvolvimento (lint/CI) do JurisControl. Não afeta o site servido, que é HTML/JS estático.",
  "scripts": {
    "lint": "eslint js/",
    "test": "vitest",
    "test:run": "vitest run",
    "test:rules": "npx --yes firebase-tools emulators:exec --only firestore --project demo-juriscontrol \"vitest run --config vitest.rules.config.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "eslint": "^9.13.0",
    "firebase": "^12.15.0",
    "globals": "^15.11.0",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
